[project]
name = "hermes-link"
version = "0.1.0"
description = "Hermes agent communication routing"
requires-python = ">=3.10"
license = "MIT"
dependencies = ["PyYAML"]

[project.scripts]
hermes_link = "hermes_link.cli:main"

[tool.setuptools.packages.find]
include = ["hermes_link*"]
